export const sortByValue = (items) => {
  return items.sort(function (a, b) {
    if (a.rating < b.rating) {
      return 1;
    }
    if (a.rating > b.rating) {
      return -1;
    }
    return 0;
  })
}